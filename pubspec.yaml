name: coderaayush
description: Simple Portofolio Site in Flutter.

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^2.0.0
  line_icons: ^2.0.1
  nil: ^1.1.1
  url_strategy: ^0.2.0

dev_dependencies:
  lint: ^1.5.3

flutter:
  uses-material-design: true
  assets:
    - assets/images/
